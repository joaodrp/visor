#!/usr/bin/env ruby

# VISoR Meta command line interface script.
# Run <visor-meta -h> to get more usage help.

require File.expand_path('../../lib/meta', __FILE__)

Visor::Common::CLI.new(Visor::Meta::Server, 'visor-meta').run
